/target
.idea
.crush
.claude
.junie
.direnv

.DS_Store
**/.DS_Store
.worktrees/
.superpowers/

.env
.env.*

# Minted at runtime by nextcloud-test/setup.sh — never commit.
nextcloud-test/.app-password

# cargo-fuzz build artifacts (nightly libFuzzer runs)
fuzz/target
fuzz/artifacts
fuzz/coverage
